(function(){'use strict';if(window.__ntPayPalReadiness)return;window.__ntPayPalReadiness=true;function $id(x){return document.getElementById(x)}function cloud(){return window.nt28Cloud||window.NT_CLOUD_CLIENT||null}function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}async function selectedBalance(mid){var sb=cloud();if(!sb||!mid)return null;var r=await sb.rpc('nursetrack_membership_balance',{p_membership_id:mid});if(r.error)return null;var row=r.data&&r.data[0];return row?Number(row.balance||0):0}function selectedMembership(){var e=$id('ntExistingMembership');return e&&e.value||''}async function refresh(){var box=$id('ntPayPalReadiness');if(!box)return;var mid=selectedMembership(),status=$id('ntPayPalStatus'),amount=$id('ntPayPalBalance'),btn=$id('ntPayPalCheckout');if(!mid){status.textContent='Selecciona una membresía para ver el estado de PayPal.';amount.textContent='Balance: —';btn.disabled=true;return}var bal=await selectedBalance(mid);amount.textContent=bal==null?'Balance: no disponible':'Balance pendiente: $'+bal.toFixed(2);var endpoint=(window.NT_PAYPAL_ENDPOINT||'').trim();if(!endpoint){status.textContent='PayPal preparado, pero el backend seguro de cobro todavía no está activado en Vercel.';btn.disabled=true;btn.textContent='PayPal pendiente de activación';return}status.textContent=bal>0?'PayPal listo para conectarse al backend seguro configurado.':'No hay balance pendiente para cobrar.';btn.disabled=true;btn.textContent=bal>0?'Abrir PayPal (requiere backend activo)':'Sin balance pendiente'}async function inject(){var page=$id('membershipPage');if(!page||$id('ntPayPalReadiness'))return;var box=document.createElement('div');box.id='ntPayPalReadiness';box.className='card';box.style.marginTop='18px';box.innerHTML='<h3>🅿️ PayPal · Cobro de membresía</h3><p class="muted">El monto se obtiene del balance real de la membresía. El secreto de PayPal nunca se guarda en el navegador.</p><div class="row" style="gap:12px;align-items:center;flex-wrap:wrap"><strong id="ntPayPalBalance">Balance: —</strong><span id="ntPayPalStatus" class="muted">Comprobando configuración…</span></div><div class="row" style="margin-top:12px"><button id="ntPayPalCheckout" class="btn primary" type="button" disabled>PayPal pendiente de activación</button></div><div class="muted" style="margin-top:8px;font-size:12px">La activación final requiere un backend serverless en Vercel con PAYPAL_CLIENT_ID, PAYPAL_CLIENT_SECRET y PAYPAL_ENV. Hasta entonces no se ejecutan cobros reales.</div>';page.appendChild(box);var e=$id('ntExistingMembership');if(e)e.addEventListener('change',refresh);await refresh()}function boot(){var tries=0,t=setInterval(function(){tries++;if($id('membershipPage')){clearInterval(t);inject()}else if(tries>60)clearInterval(t)},120)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();window.NT_PAYPAL_READINESS={refresh:refresh};})();